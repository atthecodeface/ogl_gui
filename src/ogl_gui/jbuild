(jbuild_version 1)

(copy_files# sax/*)
(copy_files# font/*)
(copy_files# animatable/*)
(copy_files# stylesheet/*)
(copy_files# obj/*)
(copy_files# widget/*)

(library (
  (name ogl_gui)
  (public_name ogl_gui)
  (wrapped true)
  (libraries 
    (batteries bigarray tgls tgls.tgl4 yojson result atcflib)
  )
))

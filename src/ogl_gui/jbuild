(jbuild_version 1)

(copy_files# utils/*)
(copy_files# sax/*)
(copy_files# animatable/*)
(copy_files# font/*)
(copy_files# stylesheet/*)
(copy_files# glprogram/*)
(copy_files# layout/*)
(copy_files# ogl_types/*)
(copy_files# texture/*)
(copy_files# ogl_obj/*)
(copy_files# widget/*)
(copy_files# app/*)

(library (
  (name ogl_gui)
  (public_name ogl_gui)
  (wrapped true)
  (libraries 
    (batteries bigarray tgls tgls.tgl4 yojson result atcflib)
  )
))

(jbuild_version 1)

(library (
  (name ogl_obj)
  (modules (ogl_obj_base ogl_obj_geometry ogl_obj_text ogl_obj))
  (libraries (
    bigarray re tgls tgls.tgl4 tsdl yojson atcflib
    utils
    font
    ogl_program
    ogl_types
  ))
))
